<%= form_with(model: cohorts_student, local: true) do |form| %>
  <% if cohorts_student.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cohorts_student.errors.count, "error") %> prohibited this cohorts_student from being saved:</h2>

      <ul>
      <% cohorts_student.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :cohort_id %>
    <%= form.number_field :cohort_id %>
  </div>

  <div class="field">
    <%= form.label :student_id %>
    <%= form.number_field :student_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

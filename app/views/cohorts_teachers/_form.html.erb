<%= form_with(model: cohorts_teacher, local: true) do |form| %>
  <% if cohorts_teacher.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cohorts_teacher.errors.count, "error") %> prohibited this cohorts_teacher from being saved:</h2>

      <ul>
      <% cohorts_teacher.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :cohort_id %>
    <%= form.number_field :cohort_id %>
  </div>

  <div class="field">
    <%= form.label :teacher_id %>
    <%= form.number_field :teacher_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
